use  a

g :: fn {
}

// f does this
f :: fn a:int, b:str -> float {
// YO
    if true { // 1
        g() = 1
    // this never happens
    } else if false { // 2
        g() = 2
    }

    if false { // 3
    // neither does this
        g() = 3
    }
    // last
}

start :: fn {
}
