def main ::= 
    let x = getX ' { x: 2, y: 3 }
    in x

def getX :: { x } = x

--+ expected stderr:
--+ lua: stdin:92: attempt to call a nil value (global '_20_getX')
--+ stack traceback:
--+ 	stdin:92: in function <stdin:91>
--+ 	stdin:91: in main chunk
--+ 	[C]: in ?

