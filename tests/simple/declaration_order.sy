def main ::= 
    let x = getX ' { x: 2, y: 3 }
    in x

def getX :: { x } = x

--+ expected stderr:
--+ lua: stdin:43: attempt to call a nil value (global '_1_getX')
--+ stack traceback:
--+ 	stdin:43: in function <stdin:42>
--+ 	stdin:42: in main chunk
--+ 	[C]: in ?

