def main ::= 
    let x = getX ' { x: 2, y: 3 }
    in x

def getX :: { x } = x

--+ expected stderr:
--+ lua: stdin:116: attempt to call a nil value (global '_21_getX')
--+ stack traceback:
--+ 	stdin:116: in function <stdin:115>
--+ 	stdin:115: in main chunk
--+ 	[C]: in ?

