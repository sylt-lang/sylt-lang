def main ::= 
    let x = getX ' { x: 2, y: 3 }
    in x

def getX :: { x } = x

--+ expected stderr:
--+ lua: stdin:91: attempt to call a nil value (global '_20_getX')
--+ stack traceback:
--+ 	stdin:91: in function <stdin:90>
--+ 	stdin:90: in main chunk
--+ 	[C]: in ?

