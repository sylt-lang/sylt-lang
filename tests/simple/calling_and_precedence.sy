mod A


type A = foreign
def a : Int -> A := Preamble.unsafe_coerce

def b : A -> Int := Preamble.unsafe_coerce

def main ::=
  (b (a 1))
  + (a 1 # b)
  + (1 # a # b)
  + (a ' 1 # b)
  + (b ' (a ' 1))
  + (b ' a 1)

--+ expected stdout: 6

