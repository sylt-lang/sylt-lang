
type A = foreign
def a : Int -> A := unsafe_coerce

def b : A -> Int := unsafe_coerce

def main ::=
  (b (a 1))
  + (a 1 # b)
  + (1 # a # b)
  + (a ' 1 # b)
  + (b ' (a ' 1))
  + (b ' a 1)

--+ expected stdout: 6

