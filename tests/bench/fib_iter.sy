// A Fibonacci implementation that is a little
// less awful. But we run it 1000 times instead.
// flags: no_print
start :: fn do
    j := 0
    loop j < 1000 do
        j += 1
        a := 0
        b := 1
        i := 0
        loop i < 50 do
            i += 1
            c := a
            a = b
            b += c
        end
        a <=> 12586269025
    end
end

