from std use (clamp)

start :: fn do
    clamp(1.0, -1.0, 2.0) <=> 1.0
    clamp(3.0, -1.0, 2.0) <=> 2.0
    clamp(-2.0, -1.0, 2.0) <=> -1.0

    clamp(1, -1, 2) <=> 1
    clamp(3, -1, 2) <=> 2
    clamp(-2, -1, 2) <=> -1
end
