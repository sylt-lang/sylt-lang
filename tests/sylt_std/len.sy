start :: fn do
    set.len(set.from_list' ["a", "b", "c"]) <=> 3
    dict.len(dict.from_list' [("a", "b")]) <=> 1
    list.len([1, 2]) <=> 2
    dict.len(dict.from_list' [(1, 2)]) <=> 1
    dict.len(dict.from_list' []) <=> 0
    set.len(set.from_list' []) <=> 0
    list.len([]) <=> 0
end

