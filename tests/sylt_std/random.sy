start :: fn do
    1 <=> randint' 1, 1

    a :: random'
    0. <= a and a <= 1. <=> true

    b :: randint' 0, 5
    0 <= b and b <= 5 <=> true

    c :: randint' -10, -5
    -10 <= c and c <= -5 <=> true
end
