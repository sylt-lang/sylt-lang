start :: fn do
    ts := 0
    t :: fn -> bool do
        ts += 1
        ret true
    end
    fs := 0
    f :: fn -> bool do
        fs += 1
        ret false
    end
    i :: fn -> bool do
        <!>
        ret false
    end
    if t() || i() && i() do
        // This block is intentionally left blank
    end else do
        <!>
    end
    if f() && i() || t() do
        // This block is intentionally left blank
    end else do
        <!>
    end
    fs <=> 1
    fs = 0
    ts <=> 2
    ts = 0
    f() && i() || f() <=> t() && f() || f()
    f() && i() || f() <=> (t() && f()) || f()
    t() && t() && f() || t() <=> t() || i() && i() && i()
    fs <=> 9
    ts <=> 6
end

