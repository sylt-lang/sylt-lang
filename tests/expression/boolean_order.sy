start :: fn do
    ts := 0
    t :: fn -> bool do
        ts += 1
        ret true
    end
    fs := 0
    f :: fn -> bool do
        fs += 1
        ret false
    end
    i :: fn -> bool do
        <!>
        ret false
    end
    if t() || i() and i() do
        // This block is intentionally left blank
    end else do
        <!>
    end
    if f() and i() || t() do
        // This block is intentionally left blank
    end else do
        <!>
    end
    fs <=> 1
    fs = 0
    ts <=> 2
    ts = 0
    f() and i() || f() <=> t() and f() || f()
    f() and i() || f() <=> (t() and f()) || f()
    t() and t() and f() || t() <=> t() || i() and i() and i()
    fs <=> 9
    ts <=> 6
end

