start :: fn do
    ts := 0
    t :: fn -> bool do
        ts += 1
        ret true
    end
    fs := 0
    f :: fn -> bool do
        fs += 1
        ret false
    end
    i :: fn -> bool do
        <!>
        ret false
    end
    if f() && i() do
        <!>
    end
    if t() || i() do
        // This block is intentionally left blank
    end else do
        <!>
    end
    if f() && i() && i() do
        <!>
    end
    if t() || i() || i() do
        // This block is intentionally left blank
    end else do
        <!>
    end
    fs <=> 2
    ts <=> 2
end

